{
  "name": "ms-emails",
  "version": "1.0.0",
  "description": "A microservice app that subscribes to another application via Kafka to learn when emails are sent and also sends emails to system admins.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --watch './' --exec 'ts-node ./main.ts' -e ts"
  },
  "author": "Luis Coelho",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^18.7.14",
    "@types/nodemailer": "^6.4.5",
    "express": "^4.18.1",
    "kafkajs": "^2.2.0",
    "nodemailer": "^6.7.8",
    "nodemon": "^2.0.19",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.2"
  }
}
